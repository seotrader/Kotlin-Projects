<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.almitasoft.wowchat">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/wowchaticon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/wowchaticon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.Activities.ForgotPasswordActivity"
            android:parentActivityName=".UI.Activities.LoginActivity"/>
        <activity
            android:name=".UI.Activities.ChatActivity"
            android:parentActivityName=".UI.Activities.DashBoardActivity" />
        <activity
            android:name=".UI.Activities.ProfileActivity"
            android:parentActivityName=".UI.Activities.DashBoardActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" /> <!-- optional (needed if default theme has no action bar) -->
        <activity android:name=".UI.Activities.StatusActivity" />
        <activity android:name=".UI.Activities.SettingsActivity" />
        <activity android:name=".UI.Activities.DashBoardActivity" />
        <activity android:name=".UI.Activities.CreateAccountActivity" />
        <activity android:name=".UI.Activities.LoginActivity"
            android:parentActivityName=".UI.Activities.MainActivity"
            />
        <activity android:name=".UI.Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>